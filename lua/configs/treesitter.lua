local options = {
  ensure_installed = { "lua", "luadoc", "vim", "vimdoc", "python", "c", },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
