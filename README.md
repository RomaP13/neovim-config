# neovim-config

Personal Neovim configuration focused on whatever...

## Features (Current)

* [x] Plugin manager setup (lazy.nvim)
* [x] Native LSP setup
* [x] Autocompletion (blink / Codeium)
* [x] Treesitter syntax highlighting (working?)
* [x] FZF (fuzzy finding) though it's trash...
* [x] Git integration (gitsigns / lazygit)

## TODO / Roadmap

### Core Improvements

* [X] Refactor config structure (split into modules)

### LSP & Completion

* [ ] Setup more LSP servers?
* [ ] Make my own plugin for Python workflow?
* [ ] Improve diagnostics UI

### Navigation & Search

* [ ] Add live grep with filters (fzf stuff...)
* [ ] Add bookmarks / marks system ???

### UI / UX

* [ ] Improve colorscheme consistency
* [ ] Fix lualine
* [ ] Better notifications (get rid of figdet?)

### Performance

* [ ] Measure startup time (`nvim --startuptime`)
* [ ] Remove unused plugins

### Experimental

* [ ] Explore Neovim's built-in plugin manager

## Bugs / Fixes

* [ ] Fix and setup FZF correctly for my own needs

## Keymaps

To implement... 

## Structure

```
~/.config/nvim/
├── .gitignore            # ignored files
├── README.md             # documentation
├── init.lua              # main config
├── lazy-lock.json        # plugin lockfile
├── stylua.toml           # formatting config

├── lsp/                  # custom LSP configs
│   └── *.lua

├── lua/
│   ├── config/           # editor behavior
│   │   ├── options.lua   # vim options
│   │   ├── keymaps.lua   # keybindings
│   │   └── autocmds.lua  # auto commands
│   │
│   ├── core/             # core systems
│   │   ├── lazy.lua      # plugin manager
│   │   ├── lsp.lua       # LSP setup
│   │   └── theme.lua     # colorscheme
│   │
│   ├── plugins/          # plugin specs/configs
│   │   └── *.lua
│   │
│   └── utils/            # helper functions
│       └── *.lua

├── scripts/              # custom scripts
│   └── pre-install.sh    # setup script(work in progress)
```

##  Notes

* Keep it simple
* Avoid unnecessary plugins
* Prefer Lua over Vimscript
* Yeah, 90% of it was generated by ChatGPT

## Inspiration

Configs and ideas inspired by the Neovim community.

Add some...

## Setup

To implement...
